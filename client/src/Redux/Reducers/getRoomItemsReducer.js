/* import { GET_ROOM_ITEMS } from '../types/types';

const getRoomItemsReducer = (state = [], action) => {
  const { type, payload } = action;
  switch (type) {
    case GET_ROOM_ITEMS:

      return payload;

    default:
      return state;
  }
};

export default getRoomItemsReducer; */
